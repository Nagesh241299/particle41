Project Overview: SimpleTimeService.

Prerequisites: * Terraform installed.

AWS CLI configured with AdministratorAccess.

Docker installed.

Deployment Steps:

cd terraform

terraform init

terraform apply -auto-approve


